name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: A minimal AWS TypeScript Pulumi program with a Landing Zone
  important: true
  config:
    aws:region:
      description: The AWS region to deploy into
      default: ap-southeast-2
    serviceName:
      description: The resource name prefix for the resources to deploy
      default: aws-syd